<UserControl x:Class="Sampler.Views.GainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel Margin="20">
        <TextBlock Text="Gain Effect" Foreground="White" FontSize="16" Margin="0,0,0,10"/>
        <Slider Minimum="0" Maximum="2" Value="{ Binding GainFactor, Mode=TwoWay }" TickFrequency="0.1" IsSnapToTickEnabled="True"/>
        <Button Content="Zastosuj" Command="{Binding ApplyGainCommand}" Margin="0,10,0,0"/>
    </StackPanel>
</UserControl>
