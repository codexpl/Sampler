<UserControl x:Class="Sampler.Views.MenuGeneratorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Sampler.ViewModels">

    <Grid Background="#222">
        <StackPanel Margin="20">
            <TextBlock Text="Wybierz efekt audio:"
                       Foreground="White"
                       FontSize="18"
                       Margin="0,0,0,10"/>

            <ListBox ItemsSource="{Binding AvailableEffects}"
                     SelectedItem="{Binding SelectedEffect}"
                     Foreground="White"
                     Background="#333"
                     BorderThickness="0"
                     Height="200"/>

            <Button Content="Otwórz efekt"
                    Command="{Binding OpenEffectCommand}"
                    Margin="0,10,0,0"
                    Padding="10,5"/>
        </StackPanel>
    </Grid>
</UserControl>


