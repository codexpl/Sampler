<UserControl x:Class="Sampler.Views.MixView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Sampler.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Button Content="Load file x" HorizontalAlignment="Left" Margin="50,55,0,0" VerticalAlignment="Top" Width="85" Command="{Binding LoadXCommand}"/>
        <Button Content="Load file x" HorizontalAlignment="Left" Margin="50,155,0,0" VerticalAlignment="Top" Width="85" Command="{Binding LoadYCommand}"/>
        <Button Content="Mix" HorizontalAlignment="Left" Margin="50,255,0,0" VerticalAlignment="Top" Width="85" Command="{Binding MixCommand}"/>
    </Grid>
</UserControl>
